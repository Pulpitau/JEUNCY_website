# Valeurs utilisees uniquement quand le build est lance avec --mode production
# (voir script "build:production" dans package.json). Meme logique que
# .env.staging (voir ce fichier pour le contexte du bug qu'il corrige) :
# evite d'avoir a editer .env a la main avant chaque build destine au vrai
# domaine de production.
VITE_API_URL=https://api.jeuncy.com/api
